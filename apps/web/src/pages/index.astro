---
import Base64Wall from "../components/Base64Wall";
import PasswordGenerator from "../components/PasswordGenerator";
import MainLayout from "../layouts/MainLayout.astro";
---

<MainLayout>
  <section class="flex relative">
    <div
      class="absolute top-0 left-0 bottom-0 right-0 text-subtle break-all p-1 text-right overflow-hidden select-none -z-10 font-mono leading-snug"
      aria-hidden="true"
    >
      <div
        class="absolute top-0 left-0 bottom-0 right-0 bg-gradient-to-r from-black to-black/70 md:to-black/50"
      >
        <div
          class="absolute top-0 left-0 bottom-0 right-0 bg-gradient-to-t from-black to-black/50 md:to-black/40"
        >
        </div>
      </div>
      <Base64Wall />
    </div>
    <div class="px-4 mx-auto md:mx-12 my-16">
      <div
        class="font-black text-5xl md:text-7xl mb-8 text-center md:text-left"
      >
        <h2 class="inline">Offline Passwords</h2>
        <br />
        <h2
          class="inline text-transparent bg-clip-text bg-gradient-to-r from-primary-400 to-secondary-400"
        >
          accessible anywhere
        </h2>
      </div>
      <span class="text-xl"
        >The password manager that doesn't save your passwords
      </span>
    </div>
  </section>
  <section class="mt-8 md:mt-24 bg-surface shadow py-16">
    <div class="md:mx-12 flex gap-8 justify-stretch flex-col md:flex-row px-4">
      <div class="md:min-w-[24rem]">
        <h3 class="text-2xl font-bold mb-6">Demo</h3>
        <PasswordGenerator client:idle />
      </div>
      <div>
        <h3 class="text-2xl font-bold mb-4">How it works</h3>
        <p>
          Cat ipsum dolor sit amet, my cat stared at me he was sipping his tea,
          too meow meow, i tell my human cat dog hate mouse eat string barf
          pillow no baths hate everything, but pretend not to be evil.
        </p>
        <p>
          Love you, then bite you tuxedo cats always looking dapper but cat
          playing a fiddle in hey diddle diddle? yet catch mouse and gave it as
          a present but break a planter, sprint, eat own hair, attack christmas
          tree and stuck jumping out of litter box and run around the house
          scream meowing.
        </p>
        <p>
          Catto munch salmono plan steps for world domination yet cat gets stuck
          in tree firefighters try to get cat down firefighters get stuck in
          tree cat eats firefighters' slippers stand in front of the computer
          screen whenever a door is opened, rush in before the human.
        </p>
      </div>
    </div>
  </section>
  <section
    class="flex gap-4 items-stretch md:items-start md:justify-around mt-12 md:mt-24 flex-col md:flex-row px-4"
  >
    {
      [
        {
          icon: "lock-line.svg",
          title: "Secure",
          text: `Based on research at Stanford university and implemented using modern cryptographic algorithms.`,
        },
        {
          icon: "globe-grid-line.svg",
          title: "Available everywhere",
          text: `Content Content Content Content Content Content Content Content Content Content Content Content Content
    Content Content Content`,
        },
        {
          icon: "flask-line.svg",
          title: "Free and Open-Source",
          text: `Content Content Content Content Content Content Content Content Content Content Content Content Content
    Content Content Content`,
        },
      ].map((card) => (
        <div class="flex-1 md:max-w-lg border rounded-lg p-4 border-white/10 shadow">
          <div class="flex items-center mb-2">
            {card.icon && (
              <img
                src={card.icon}
                alt={card.icon}
                class="mr-4 w-8 h-8 invert select-none"
              />
            )}
            <h4 class="text-xl font-bold flex-1">{card.title}</h4>
          </div>
          <p>{card.text}</p>
        </div>
      ))
    }
  </section>
  <section>
    <!-- CALL TO ACTION -->
  </section>
</MainLayout>
